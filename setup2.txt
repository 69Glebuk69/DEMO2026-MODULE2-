====================================================================================
# Поздравляю! Вы скачали данный файл для конфигурации всех ВМ для МОДУЛЯ 2
# предназначенного для демоэкзамена 2026 года на специальность 
# 09.02.06 Сетевое и системное администрирование
# Теперь ваша задача - это не спалиться на том, что вы его используете
# Сделать это можно в несколько шагов:
# 1. Переместите этот файл в папку tmp (mv название_файла.txt tmp/)
# 2. Удалите curl (apt-get remove curl)
# 3. Очистите историю при помощи команды history -c
# 4. Выйдете из пользователя и после обратно войдите
# 5. Очистите историю прошлой сессии (nano ~/.bash_history)
# 6. Удалите все трочки, КРОМЕ первых двух
# 7. ПОСЛЕ ВЫПОЛНЕНИЯ ВСЕХ ОПЕРАЦИЙ НЕ ЗАБУДЬТЕ УДАЛИТЬ ФАЙЛ ИЗ ПАПКИ tmp 
# (rm -f tmp/название_файла.txt) 
# Поздравляю! Вы успешно замели все следы!
====================================================================================

# ПЕРВОЕ и самое ГЛАВНОЕ, что мы делаем это добавляем в resolv.conf новый nameserver
# BR-SRV
echo "nameserver 192.168.1.10" > /etc/resolv.conf
echo "nameserver 192.168.1.10" > /etc/net/ifaces/enp7s1/resolv.conf
systemctl restart network
# Далее, перезагружаем frr на машинах HQ-RTR и BR-RTR
# HQ-RTR и BR-RTR
systemctl restart frr.service
# Поздравляю! Первоначальная настройка выполнена!
# Приступим к выполнению заданий.

### Задание 1. Настройте контроллер домена Samba DC на сервере BR-SRV

# BR-SRV
# если не работает apt-get, то
# echo "nameserver 77.88.8.8" >> /etc/resolv.conf
# echo "nameserver 77.88.8.8" >> /etc/net/ifaces/enp7s1/resolv.conf

# BR-SRV
# Устанавливаем пакет task-samba-dc
apt-get update && apt-get install -y task-samba-dc
# Настраиваем контроллер домена
rm -f /etc/samba/smb.conf
rm -rf /var/lib/samba/
rm -rf /var/cache/samba/
mkdir -p /var/lib/samba/sysvol
samba-tool domain provision
# Всё скипаем и устанавливаем пароль администратора P@ssw0rd

# Настраиваем Kerberos
\cp /var/lib/samba/private/krb5.conf /etc/krb5.conf
systemctl enable --now samba.service
echo "search au-team.irpo" > /etc/net/ifaces/enp7s1/resolv.conf
echo "nameserver 127.0.0.1" » /etc/net/ifaces/enp7s1/resolv.conf
systemctl restart network
# Проверяем
samba-tool domain info 127.0.0.1
kinit Administrator
# Если забыли пароль: samba-tool user setpassword Administrator

klist

# Настраиваем группу и пользователей
samba-tool group add hq
for i in {1..5}; do 
    samba-tool user add hquser$i Pssw0rd; 
    samba-tool user setexpiry hquser$i --noexpiry; 
    samba-tool group addmembers "hq" hquser$i; 
done
#Проверить
samba-tool group listmembers hq

### Задание 2. Сконфигурируйте файловое хранилище на сервере HQ-SRV

# HQ-SRV
# Настраиваем RAID-массив
mdadm --create /dev/md0 -l 0 -n 2 /dev/sdb /dev/sdc
mdadm --detail --scan --verbose | tee -a /etc/mdadm.conf
mkfs.ext4 /dev/md0
echo "/dev/md0 /raid ext4 defaults 0 0" » /etc/fstab
mkdir /raid
mount -av

# Проверить:
lsblk
cat /etc/mdadm.conf
blkid /dev/md0


### Задание 3. Настройте сервер сетевой файловой системы (nfs) на HQ-SRV
# HQ-SRV
# Устанавливаем пакет nfs-server
# если не работает apt-get, то
# echo "nameserver 77.88.8.8" > /etc/resolv.conf
# echo "nameserver 77.88.8.8" > /etc/net/ifaces/enp7s1/resolv.conf

apt-get update && apt-get install -y nfs-server

# Настраиваем NFS-сервер (ip могут отличаться)
mkdir /raid/nfs
chmod -R 777 /raid/nfs
echo "/raid/nfs 192.168.2.0/24(rw,no_root_squash)" > /etc/exports
exportfs -arv
systemctl enable --now nfs-server.service

#HQ-CLI (ip могут отличаться)

mkdir /mnt/nfs
chmod -R 777 /mnt/nfs
echo "192.168.1.10:/raid/nfs /mnt/nfs nfs defaults,_netdev 0 0" » /etc/fstab 
mount -av

# Проверить
df -h
echo "Hello" > /mnt/nfs/test.txt
cat /mnt/nfs/test.txt

### Задание 4. Настройте службу сетевого времени на базе
# Сервиса chrony на маршрутизаторе ISP

# ISP
# Настраиваем NTP-сервер
sed -i 's/^pool/#pool/' /etc/chrony.conf
cat <<EOF > /etc/chrony.conf
server ntp0.ntp-servers.net iburst prefer minstratum 4
local stratum 5
allow 0.0.0.0/0
EOF
systemctl restart chronyd
# Проверить
chronyc tracking

# HQ-RTR
# Настраиваем NTP-клиент
sed -i "s/^pool/#pool/" /etc/chrony.conf
echo "server 172.16.2.1 iburst" >> /etc/chrony.conf
systemctl restart chronyd
# Проверить
chronyc sources

# BR-RTR
# Настраиваем NTP-клиент
sed -i "s/^pool/#pool/" /etc/chrony.conf
echo "server 172.16.2.1 iburst" >> /etc/chrony.conf
systemctl restart chronyd
# Проверить
chronyc sources

# HQ-SRV
# Настраиваем NTP-клиент
sed -i "s/^pool/#pool/" /etc/chrony.conf
echo "server 172.16.1.1 iburst" >> /etc/chrony.conf
systemctl restart chronyd
# Проверить
chronyc sources

# BR-SRV
# Настраиваем NTP-клиент
sed -i "s/^pool/#pool/" /etc/chrony.conf
echo "server 172.16.2.1 iburst" >> /etc/chrony.conf
systemctl restart chronyd
# Проверить
chronyc sources

# HQ-CLI
# Настраиваем NTP-клиент
sed -i "s/^pool/#pool/" /etc/chrony.conf 
echo "server 172.16.1.1 iburst" >> /etc/chrony.conf
systemctl restart chronyd
# Проверить
chronyc sources

### Задание 5. Сконфигурируйте Ansible на сервере BR-SRV.

# BR-SRV
# Устанавливаем пакеты ansible и sshpass
apt-get update && apt-get install -y ansible sshpass

# Настраиваем ansible (Сначала первый, потом второй):
cat <<EOF > /etc/ansible/ansible.cfg
[defaults]
inventory = /etc/ansible/hosts
host_key_checking = False
EOF

# Вот, второй
cat <<EOF > /etc/ansible/hosts
HQ-SRV ansible_host=192.168.1.10 ansible_user=sshuser ansible_password=P@ssw0rd ansible_port=2026
HQ-CLI ansible_host=192.168.2.10 ansible_user=user ansible_password=resu
HQ-RTR ansible_host=172.16.1.10 ansible_user=net_admin ansible_password=P@ssw0rd
BR-RTR ansible_host=172.16.2.10 ansible_user=net_admin ansible_password=P@ssw0rd

[all:vars]
ansible_python_interpreter=/usr/bin/python3
EOF

# Проверить
cd /etc/ansible/
ansible -m ping all

#Запустить на всех машинах ssh
systemctl start sshd.service

### Задание 6. Разверните веб-приложение testapp
# c использованием средств контейнеризации на сервере BR-SRV

# BR-SRV
# Устанавливаем пакеты docker-engine и docker-compose-v2
apt-get update && apt-get install -y docker-engine docker-compose-v2
systemctl enable --now docker.service
# Загружаем необходимые образы
mount /dev/sr0 /mnt/
docker load < /mnt/docker/site_latest.tar
docker load < /mnt/docker/mariadb_latest.tar
# Проверить
docker image ls

# Настроиваем Docker Compose
cat <<EOF > compose.yaml
services:
  database:
    container_name: db
    image: mariadb:10.11
    restart: always
    ports:
      - "3306:3306"
    environment:
      MARIADB_DATABASE: "testdb"
      MARIADB_USER: "testc"
      MARIADB_PASSWORD: "P@ssw0rd"
      MARIADB_ROOT_PASSWORD: "toor"

  app:
    container_name: testapp
    image: site:latest
    restart: always
    ports:
      - "8000:8000"
    environment:
      DB_TYPE: "maria"
      DB_HOST: "192.168.3.10"
      DB_PORT: "3306"
      DB_NAME: "testdb"
      DB_USER: "testc"
      DB_PASS: "P@ssw0rd"
    depends_on:
      - database
EOF
# Запускаем описанный стек контейнеров
docker compose up -d
# Проверить
docker compose ps

# HQ-CLI
# Проверить работоспособность приложения
# (зайти на сайт 192.168.3.10:3306)

### Задание 7. Разверните веб приложение на сервере HQ-SRV

# HQ-SRV:
# Устанавливаем пакет lamp-server:
apt-get update && apt-get install -y lamp-server
# Копируем необходимые файлы с образа Additional.iso:
mount /dev/sr0 /mnt/
cp /mnt/web/index.php /var/www/html/
cp /mnt/web/logo.png /var/www/html/
# Настраиваем файл index.php:
sed -i 's/$username = "user"/$username = "webc"/' /var/www/html/index.php
sed -i 's/$password = "password"/$password = "P@ssw0rd"/' /var/www/html/index.php
sed -i 's/$dbname = "db"/$dbname = "webdb"/' /var/www/html/index.php
# Настраиваем MariaDB:
systemctl enable --now mariadb
mariadb -u root
CREATE DATABASE webdb;
CREATE USER 'webc'@'localhost' IDENTIFIED BY 'P@ssw0rd';
GRANT ALL PRIVILEGES ON webdb.* TO 'webc'@'localhost' WITH GRANT OPTION;
EXIT;
mariadb -u webc -p -D webdb < /mnt/web/dump.sql
# Потом надо написать P@ssw0rd

# Запускаем веб-сервер Apache2:
systemctl enable --now httpd2.service

# HQ-CLI:
# Проверить доступ до веб-сайта: ввести IP на клиенте в браузере
# Ввести в браузере 192.168.1.10

### Задание 8. На маршрутизаторах сконфигурируйте статическую трансляцию портов

# HQ-RTR:
# Обнавляем пакеты и устанвливаем iptables
apt-get update && apt-get install -y iptables

# Настраиваем пробросы портов:
iptables -t nat -A PREROUTING -i enp7s1 -p tcp --dport 2026 -j DNAT --to-destination 192.168.1.10:2026
iptables -t nat -A PREROUTING -i enp7s1 -p tcp --dport 8080 -j DNAT --to-destination 192.168.1.10:8080
iptables-save >> /etc/sysconfig/iptables
# Проверить:
iptables -t nat -L -n -v

# BR-RTR:
# Обнавляем пакеты и устанвливаем iptables
apt-get update && apt-get install -y iptables

# Настраиваем пробросы портов:
iptables -t nat -A PREROUTING -i enp7s1 -p tcp --dport 2026 -j DNAT --to-destination 192.168.3.10:2026
iptables -t nat -A PREROUTING -i enp7s1 -p tcp --dport 8080 -j DNAT --to-destination 192.168.3.10:8080
iptables-save >> /etc/sysconfig/iptables
# Проверить:
iptables -t nat -L -n -v

# ISP:
# Проверить функциональность проброса портов:
ssh -p 2026 sshuser@172.16.1.10 
exit
ssh -p 2026 sshuser@172.16.2.10 
exit
curl http://172.16.1.10:8080 | head
curl http://172.16.2.10:8080 | head

### Задание 9. Настройте веб-сервер nginx как обратный прокси-сервер на ISP

# ISP:
# Устанавливаем пакет nginx:
apt-get update && apt-get install -y nginx
# Настраиваем nginx:
cat «EOF > /etc/nginx/sites-available.d/default.conf
server {
    listen 80;
    server_name web.au-team.irpo;
    location / {
        proxy_pass http://192.168.1.10:8080;
    }
}

server {
    listen 80;
    server_name docker.au-team.irpo;
    location / {
        proxy_pass http://192.168.3.10:8080;
    }
}

web.au-team.irpo/ 
docker.au-team.irpo:8080
EOF
ln -s /etc/nginx/sites-available.d/default.conf /etc/nginx/sites-enabled.d/
systemctl enable --now nginx

# HQ-CLI:
# Настраиваем файл /etc/hosts
su - 
echo "172.16.1.1 web.au-team.irpo" » /etc/hosts
echo "172.16.2.1 docker.au-team.irpo" » /etc/hosts
exit
# Проверяем доступность веб-ресурсов по требуемым именам: 
# web.au-team.irpo; docker.au-team.irpo (в браузере вводим имена)

### Задание 10. На маршрутизаторе ISP настройте web-based аутентификацию

# Устанавливаем пакет apache2-htpasswd:
apt-get update && apt-get install -y apache2-htpasswd

# Настраиваем аутентификацию:
htpasswd -c /etc/nginx/.htpasswd WEB (пароль: P@ssw0rd)

# Перезагружаем nginx.service
systemctl restart nginx.service
# Проверка. Заходим на HQ-CLI и пытаемся зайти на сайт web.au-team.irpo
# Если потребовало пароль, значит всё сделано правильно

### Задание 11. Удобным способом установите Яндекс Браузер на HQ-CLI

# Переходим в root
su -

# Устанавливаем Яндекс Браузер
apt-get update && apt-get install -y yandex-browser-stable

# Выходим из рута
exit
# Далее проверяем наличие браузера на вашей машине
